
1：工厂角色（FruitsFactory）：
    简单工厂模式的核心，负责创建所有实例的内部逻辑，工厂类可以被外界直接调用，创建所需的产品对象
2：抽象产品角色（Fruits）：
    简单工厂模式所创建的所有对象的父类，它负责描述所有实例所共有的公共接口
3：具体产品角色（Apple）：
    简单工厂模式的创建目标，所有创建的对象都是充当这个角色的某个具体类的实例


优点：做到了和客户端的解耦。
缺点：没当有新的产品的，就需要修改工厂类的switch来扩展产品违背了开闭原则。所以请看factory-kit（抽象工厂方法）